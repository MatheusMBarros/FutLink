version: '3.8'
services:
auth-service:
    build: ./auth-service
    ports:
    - "3001:3001"
    volumes:
        - ./auth-service:/app
        - /app/node_modules
    environment:
        JWT_SECRET: your_secret_key
    restart: always