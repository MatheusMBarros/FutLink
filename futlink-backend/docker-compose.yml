version: "3.8"

services:
  user-service:
    build: ./user-service
    ports:
      - "3000:3000"
    volumes:
      - ./user-service:/app
      - /app/node_modules
    environment:
      NODE_ENV: production
    restart: always
